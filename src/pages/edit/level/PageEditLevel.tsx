import { ReactElement } from "react";
import { MainLayout } from "../../../layout/MainLayout";

const PageEditLevel = (): ReactElement => {
    return <MainLayout>Comming soon</MainLayout>;
};

export default PageEditLevel;
